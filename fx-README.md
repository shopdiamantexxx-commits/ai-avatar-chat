# fx

## 生産時間記録ツール

機種ごとの作業時間を記録するためのシンプルなWebアプリです。ビルド不要で、ブラウザだけで動作します。

### 使い方

`index.html` をブラウザで開いてください（もしくは任意の静的サーバーで配信してください）。

```bash
python3 -m http.server 8000
# ブラウザで http://localhost:8000 を開く
```

### 主な機能

- タイマーによる作業時間の計測（開始 / 一時停止 / 終了して記録）
- 機種名は選択方式の絞り込みコンボボックスから入力（頭文字だけでなく型式途中の文字列でも候補が絞られる。前方一致を優先表示）
- 手入力での記録追加（作業時間を分単位で直接入力）
- 記録の一覧表示・日付での絞り込み・個別削除・全削除
- 本日 / 直近7日間 / 全期間の合計時間と最多機種の集計表示
- 記録のCSV出力

### 機種名の一覧を変更する場合

`js/app.js` 冒頭の `MODEL_LIST_RAW` 配列を編集してください。

### データの保存先

記録はブラウザの `localStorage` に保存されます。サーバーへの送信は行われません。
